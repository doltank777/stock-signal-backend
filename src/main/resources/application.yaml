spring:
  profiles:
    active: local

kis:
  daily-price:
    finalization:
      scheduler:
        enabled: false
        cron: "-"
        zone: "Asia/Seoul"
      startup-recovery:
        enabled: false
